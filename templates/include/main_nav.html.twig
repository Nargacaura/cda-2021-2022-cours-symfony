<nav class="nav">
    <ul style="list-style: none; li { display: inline; padding: 0 3rem }">
        <li>
            <a href="/">Home</a>
        </li>
        <li>
            <a href="/annonce">Annonces</a>
        </li>
    </ul>

    {# <button class="btn btn-primary ml-1" type="button" data-toggle="collapse" data-target="#search" aria-expanded="false" aria-controls="collapseExample">
		Recherche avancée
	</button>

	<div class="collapse" id="search">
		<div class="card card-body">
			{{ render_esi(controller('App\\Controller\\AnnonceController::search')) }}
		</div>
	</div> #}

    {% if app.user %}
        <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
    {% else %}
        <a class="dropdown-item" href="{{ path('app_login') }} " style="padding: 0 3rem;">Login</a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="{{ path('app_register') }}" style="padding: 0 3rem;">Register</a>
    {% endif %}

    <select name="search_by_tag" class="selector">
            <option value="-1">Tags</option>
        {% for tag in global_tags %}
            <option {% if selectedTagID is defined and selectedTagID == tag.id %}selected{% endif %}
             value="{{ tag.id }}">{{ tag.name }}</option>
        {% endfor %}
    </select>
</nav>
